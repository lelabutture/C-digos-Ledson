//CRIAR O SERVIDOR
const express = require("express");
const app = express();
const PORT = 8000;
//CRIAR ROTAS
app.get("/", (req, res) =>{
  res.send("<p>paragrafo...</p>");

});

//RODAR SERVIDOR NUMA DADA PORTA
app.listen(PORT, () => {
  console.log(`Servidor rodando na porta", ${PORT}`);
});